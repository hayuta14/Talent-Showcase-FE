'use client';

import { Box, Typography } from '@mui/material';

export default function AdminPosts() {
  return (
    <Box>
      <Typography variant="h5">Quản lý bài đăng (đang phát triển)</Typography>
    </Box>
  );
} 